Basically just fish config at this point.

### Setup
* git clone
* replace `~/.config/fish` with a symlink, e.g. ` ln -s fish ~/dev/dotfiles/fish/`

### To Do
* Maybe a brewfile
* Maybe a list of other tools I use
